name: Legacy Trade Workflow Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Trading workflow disabled
        run: |
          echo "ATZMA trading execution no longer runs on GitHub Actions."
